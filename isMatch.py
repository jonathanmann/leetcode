class Solution:	
	def isMatch(self,s,p):
            if s == p:
                return True
            sp = ['.','*']
            for i in range(len(p)):
                if p[i + 1] == '*'
                    
s = Solution()
print s.isMatch(919)
